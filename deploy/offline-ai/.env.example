# Copy to  deploy/offline-ai/.env  and adjust. Only used by docker-compose.
# The real secrets (Tavily key, LAN IP) live in data/.config.yaml, not here.

# Timezone for the server container (affects "what time is it" answers).
TZ=America/Los_Angeles
